sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t arogozan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true arogozan/docker-react npm run test
